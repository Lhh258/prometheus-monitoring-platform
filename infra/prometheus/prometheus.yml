global:
  scrape_interval: 15s

# 新增
rule_files:
  - "rules/*.yml"

# 新增
alerting:
  alertmanagers:
    - static_configs:
        - targets:
            - alertmanager:9093

scrape_configs:
  - job_name: 'node-exporter'
    static_configs:
      - targets:
          - node-exporter:9100
